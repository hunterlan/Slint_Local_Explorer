import {FileForm} from "./file_form.slint";
import { FilePicker } from "./file_form.slint";

export {FilePicker}

export component MainWindow inherits Window {
    title: "Sling Local Explorer";
    always-on-top: true;
    width: 350px;
    height: 400px;
    FileForm {

    }
}